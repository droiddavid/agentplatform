name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          # Use GitHub Secrets to provide sensitive credentials.
          MYSQL_ROOT_PASSWORD: ${{ secrets.MYSQL_ROOT_PASSWORD }}
        ports:
          - 3306:3306
        options: >-
          --health-cmd "mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Wait for MySQL
        env:
          MYSQL_ROOT_PASSWORD: ${{ secrets.MYSQL_ROOT_PASSWORD }}
        run: |
          for i in {1..30}; do
            mysql -h 127.0.0.1 -P 3306 -u root -p"${MYSQL_ROOT_PASSWORD}" -e "SELECT 1" && break || sleep 2
          done

      - name: Create database
        env:
          MYSQL_ROOT_PASSWORD: ${{ secrets.MYSQL_ROOT_PASSWORD }}
        run: |
          mysql -h 127.0.0.1 -P 3306 -u root -p"${MYSQL_ROOT_PASSWORD}" -e "CREATE DATABASE IF NOT EXISTS agentplatform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

      - name: Build and run Flyway migrations
        env:
          FLYWAY_PASSWORD: ${{ secrets.MYSQL_ROOT_PASSWORD }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
        run: |
          ./mvnw -B -DskipTests=true org.flywaydb:flyway-maven-plugin:9.22.0:migrate -Dflyway.url=jdbc:mysql://127.0.0.1:3306/agentplatform -Dflyway.user=root -Dflyway.password=${FLYWAY_PASSWORD}

      - name: Build
        env:
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
        run: |
          ./mvnw -B -DskipTests=true clean package
