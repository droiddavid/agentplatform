# Current Objective

Build the version 1 full-stack runtime AI agent platform described in the project specification.

The current objective is to implement the application in a way that is:

- architecture-consistent
- approval-first
- data-driven
- suitable for non-technical users
- ready to evolve into a complete SPA plus PWA

The implementation agent must always align current work with:

- `/agent/core-skills/`
- `/agent/project-spec/`
- `/agent/task-context/`
